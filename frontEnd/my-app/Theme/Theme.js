import { useState } from "react";
const lightTheme = {
    backgroundColor: 'white',
    textColor: '#02092B',
  };
  
  const darkTheme = {
    backgroundColor: '#000000',
    textColor: 'white',
  };
  
  export { lightTheme, darkTheme };